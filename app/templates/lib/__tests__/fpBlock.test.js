const assert = require("assert");
const game = require("../index.js");

describe("fpSpace", () => {
  it("init return missilePanel", () => {
    assert(game !== undefined, "should be defined game");
  });
});
